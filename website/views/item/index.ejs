<div ng-app="mpmApp">
	<div ng-controller="ListController as list">
		<div class="uk-container-center uk-text-center" ng-show="list.loader">
			<img src="/images/loader.gif" alt="Loading..." width="128" height="128" />
		</div>
		<div class="uk-alert uk-alert-large uk-alert-danger" ng-show="list.error">
			<h2>An error occured</h2>
			<p>We can't reach the app, try to fix the problem by checking the following points:</p>
			<ul>
				<li>Check your internet connection</li>
				<li>Check if JavaScript is enabled on your browser</li>
				<li>Reload the page</li>
			</ul>
			<p>If everything is correct on your side, please try later. The issue may be temporary. If the issue persist, contact us.</p>
		</div>
		<div class="uk-grid-width-1-2" ng-show="list.items.length" id="containerItems">
			<div ng-repeat="item in list.items">
				<div class="uk-panel uk-panel-box uk-panel-box-secondary {{ item.infos_displayed ? 'box_highlighted' : '' }}">
					<form class="uk-form uk-form-horizontal">
						<fieldset>
							<legend>{{ item.title }}</legend>
							<div class="uk-form-row">
								<label class="uk-form-label">URL</label>
								<div class="uk-form-controls uk-form-controls-text">
									{{ item.url }}
								</div>
							</div>
							<div class="uk-form-row">
								<label class="uk-form-label">Username</label>
								<div class="uk-form-controls uk-form-controls-text">
									{{ item.username }}
								</div>
							</div>
							<div class="uk-form-row">
								<label class="uk-form-label">Password</label>
								<div class="uk-form-controls uk-form-controls-text">
									{{ item.password_hidden }} <button class="uk-button uk-button-primary uk-button-small" ng-click="list.togglePassword(item)">{{ item.pwd_displayed ? "Hide" : "Show" }}</button>
									<mpm-progress item="item"></mpm-progress>
								</div>
							</div>
							<div class="uk-form-row" ng-show="item.infos_displayed">
								<label class="uk-form-label">Comment</label>
								<div class="uk-form-controls uk-form-controls-text">
									{{ item.comment }}
								</div>
							</div>
						</fieldset>
					</form>
					<p class="uk-text-right"><a href="" ng-click="list.toggleInfos(item)">{{ item.infos_displayed ? "Less" : "More" }}</a></p>
				</div>
			</div>
		</div>
		<div ng-show="list.items.length == 0">
			<p>There is no item to display.</p>
		</div>
	</div>
</div>